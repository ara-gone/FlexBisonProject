struct Name {
	// this is a set of declarations
	string first_name,
	string last_name
}
struct Employee {
	Name name
}
struct Dept {
	Employee head
}

void main() { 
	// this is a procedure
	bool b;
	int y;
	y = 5;
	x = 6;
	y = "Hello World";
	f();
}

int f() {
	int y;
	Name aName;
	aName.firstName = "Jordan"; // need to add symbol table check for structs or else double is seen as ID
	y = 6;
}